title:
  de: Neuerscheinungen
  en: Recommendation lists

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    sidebar:
      sidebar_fields:
        type: fields
        fields:
          cover: fields/cover

  # PDF editions
  tab_editions:
    extends: tabs/files
    columns:
      column_files:
        sections:
          images: false

          files:
            extends: sections/documents
            parent: site.find('lesetipps')

  # Metadata
  tab_seo: tabs/seo
