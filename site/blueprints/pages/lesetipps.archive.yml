title:
  de: Neuerscheinungen
  en: Novelty books

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    sidebar:
      sidebar_fields:
        type: fields
        fields:
          cover: fields/cover

  # Subpages
  tab_subpages:
    extends: tabs/subpages
    label:
      de: Jahrgänge
      en: Volumes

    drafts:
      template: lesetipps.volume

    unlisted:
      template: lesetipps.volume
      sortBy: title desc
      info: "{{ page.files.filterBy('extension', 'pdf').count }} Ausgabe(n)"

    listed: false

  # Metadata
  tab_seo: tabs/seo
