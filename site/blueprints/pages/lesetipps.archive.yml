title:
  de: Neuerscheinungen
  en: Recommendation lists

options: options/default

tabs:
  tab_content:
    extends: migrate/tabs/content
    sidebar:
      sidebar_fields:
        type: fields
        fields:
          cover: migrate/fields/cover

  # Documents
  tab_editions:
    extends: migrate/tabs/media
    columns:
      column_files:
        sections:
          files:
            extends: migrate/sections/documents
            parent: site.find('lesetipps')

  # Metadata
  tab_seo: migrate/tabs/seo
