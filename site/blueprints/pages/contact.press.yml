title:
  de: Presseinfos
  en: Press infos

options: options/default

tabs:
  tab_content:
    extends: migrate/tabs/content

    fields:
      details:
        label: Details
        type: markdown

      layouts: migrate/fields/base/layouts

    sidebar:
      sidebar_fields:
        type: fields
        fields:
          downloads: custom/contact/downloads

  # Metadata
  tab_seo: tabs/seo
