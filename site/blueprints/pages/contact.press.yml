title:
  de: Presseinfos
  en: Press infos

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    fields:
      details:
        label: Details
        type: markdown

      layouts: fields/base/layouts

    sidebar:
      sidebar_fields:
        type: fields
        fields:
          downloads: custom/contact/downloads

  # Metadata
  tab_seo: tabs/seo
