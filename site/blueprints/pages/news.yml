title:
  de: Startseite
  en: Home

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    sidebar:
      sidebar_fields:
        type: fields
        fields:
          cover:
            extends: fields/cover
            image:
              ratio: 16/9

  # News
  tab_posts: tabs/custom/news

  # Metadata
  tab_seo: tabs/seo
