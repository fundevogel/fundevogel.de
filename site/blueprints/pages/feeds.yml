title: Feeds

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    fields:
      details:
        type: markdown

    sidebar:
      sidebar_fields:
        type: fields
        fields:
          cover: fields/cover

  # Metadata
  tab_seo: tabs/seo
