title: Standard

options: options/default

tabs:
  tab_content:
    extends: tabs/content
    fields:
      text: false

      layouts: fields/layouts

    sidebar:
      sidebar_fields:
        type: fields
        fields:
          date:
            extends: fields/date
            label:
              de: Zuletzt aktualisiert
              en: Last updated

  # Metadata
  tab_seo: seo
  tab_seo1: tabs/seo
