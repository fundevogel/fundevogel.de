title:
  de: Gitterliste
  en: Grid list

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    fields:
      cards:
        label:
          de: Einträge
          en: Entries
        type: structure
        fields:
          entry:
            label: Text
            type: markdown

    sidebar:
      sidebar_fields:
        type: fields
        fields:
          cover: fields/cover

  # Metadata
  tab_seo: tabs/seo
