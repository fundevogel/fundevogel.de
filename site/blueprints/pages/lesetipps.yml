title:
  de: Lesetipps
  en: Reading tips

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    sidebar:
      sidebar_fields:
        type: fields
        fields:
          fallback:
            extends: fields/cover
            label:
              de: Reservebild
              en: Fallback image
            image:
              cover: false

  # Lesetipps
  tab_posts: tabs/custom/lesetipps

  # PDF editions
  tab_editions:
    extends: tabs/subpages
    label:
      de: Jahrgänge
      en: Volumes

    drafts:
      template: lesetipps.editions
      parent: site.find('lesetipps/neuerscheinungen')

    unlisted:
      template: lesetipps.editions
      parent: site.find('lesetipps/neuerscheinungen')
      sortBy: title desc
      info: "{{ page.files.filterBy('extension', 'pdf').count }} Ausgabe(n)"

    listed: false

  # Metadata
  tab_seo: tabs/seo
