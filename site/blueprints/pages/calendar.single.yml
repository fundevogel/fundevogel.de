title:
  de: Jährliche Höhepunkte
  en: Annual highlights

tabs:
  tab_content:
    extends: tabs/content

    fields:
        text:
          label: Text
          type: markdown

        short:
          extends: fields/short
          help:
            de: Erscheint auf der 'Kalender'-Seite.
            en: Displayed on 'calendar' page.

        layouts: fields/layouts

    sidebar:
      sidebar_fields:
        extends: sections/sidebar.cover
        fields:
          previewImage:
            extends: fields/single-cover
            label:
              de: Vorschaubild
              en: Preview image

  # Metadata
  tab_seo: tabs/seo
