title:
  de: Bücher-Suche
  en: Book search

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    fields:
      details:
        label: Details
        type: markdown

    sidebar:
      sidebar_fields:
        type: fields
        fields:
          cover: fields/cover

          limit_posts: fields/groups/limit-posts

  # Metadata
  tab_seo: tabs/seo
