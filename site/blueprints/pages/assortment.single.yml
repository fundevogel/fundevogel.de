title:
  de: Sortimentskategorie
  en: Assortment category

options: options/default

tabs:
  # Content
  tab_content:
    extends: tabs/content
    fields:
      layouts: fields/base/layouts

      favorites: custom/assortment/favorites

    sidebar:
      sidebar_fields:
        type: fields
        fields:
          cover: fields/cover

          short: fields/short

  # Books
  books: tabs/books

  # Metadata
  tab_seo: tabs/seo
