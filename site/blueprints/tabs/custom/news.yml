extends: tabs/posts
columns:
  right:
    sections:
      posts_drafts:
        templates:
          - news.article
          - news.recommendation

      posts_unlisted:
        templates:
          - news.article
          - news.recommendation

  full:
    sections:
      posts_listed:
        templates:
          - news.article
          - news.recommendation
